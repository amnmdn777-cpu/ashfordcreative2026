# DEMO MODE — VITE_MOCK_AUTH=true lets ANY username/password sign in to /admin
# and /sales WITHOUT a backend. Vite inlines this at build time (production mode).
# Remove this file (or set it to false) and rebuild before a real launch.
VITE_MOCK_AUTH=true
